# シナリオCSV 作成GUI 仕様書

## 0. この文書について

[`SPEC.md`](./SPEC.md) で定義したシナリオCSV（1 ファイル 2 セクション）を、
手打ちではなく **GUI で安全に作成・編集** するためのデスクトップアプリの仕様。

GUI は「CSV を正しい形式で読み書き・検証するエディタ」に徹する。
音声生成（`infer.py` 実行）や `.ks` 連携は **行わない**（CLI 側の担当）。

---

## 1. 目的とゴール

- LoRA パス・参照番号・顔文字付きテキストを、**表計算アプリより崩れにくい形**で入力できる。
- 参照番号を人が手で合わせる必要をなくす（キャラは名前で選ぶ → 番号は自動）。
- 出力ファイル名（`ヘッド + 連番 + .wav`）を**入力しながらプレビュー**できる。
- 保存すると `SPEC.md` §4 準拠の CSV が出力され、CLI がそのまま読める。

### ゴール（Done の定義）

1. 空の状態からキャラ定義とセリフを入力し、保存 → CLI が読める CSV が生成される。
2. 既存の準拠 CSV を開いて編集し、保存し直しても形式が壊れない（ラウンドトリップ安全）。
3. 参照番号の不整合・空テキスト・LoRA フォルダ不在などを、保存前に画面上で検知できる。

---

## 2. スコープ

### 対象に含む（In Scope）

- キャラクター定義の管理（追加/編集/削除、LoRA フォルダの参照ボタン、参照番号の自動採番）。
- セリフの管理（キャラをドロップダウン選択、原文・送信テキスト入力、行の並べ替え）。
- **原文 → 送信テキストの自動反映（一方向）**（§5.2.1）。
- 出力ファイル名のライブプレビュー。
- 顔文字（絵文字）挿入パレット（意味付き 39 種、§5.3 / 付録 B）。
- CSV の新規/開く/保存/名前を付けて保存（文字コード選択、`SPEC.md` §4 形式）。
- リアルタイム検証とエラー表示。
- **選択行のお試し生成と試聴**（§5.6）。
- **一括生成のフォルダ出力（音声 + CSV 同梱）と進捗表示**（§5.5）。

### 対象に含まない（Out of Scope）※将来拡張は §11

- 音声生成ロジックそのもの（`infer.py` 実行）。実処理は CLI が担当し、GUI はそれを **起動する**だけ（§5.5/§5.6）。
- `.ks` の解析・書き戻し。
- LoRA の学習・管理。
- 波形表示・本格的な音声編集（お試し生成した wav の**再生**は行う）。

---

## 3. 技術スタック（推奨）

| 項目 | 選定 | 理由 |
|---|---|---|
| 言語 | Python 3.10+ | CLI と同一エコシステム。CSV/検証ロジックを共有できる。 |
| GUI | **PySide6（Qt6）**（確定） | `QTableView` によるインライン編集・ドロップダウン・行並べ替え・絵文字表示が容易。 |
| 音声再生 | **QtMultimedia**（`QMediaPlayer`/`QSoundEffect`） | お試し生成した wav の試聴（§5.6）。 |
| CSV/検証 | CLI と **共有ライブラリ** | 形式・検証の二重実装とズレを防ぐ（§9）。 |
| 配布 | PyInstaller | Windows 向け単一 `.exe` 化。非エンジニアでも起動可能。 |

---

## 4. 画面レイアウト

**ツールバー ＋ 3 タブ構成**。一番作業する「セリフ」を主役にし、あまり見ない
「キャラクター定義」「検証／生成ログ」は別タブへ分ける。

```
┌─ Irodori シナリオCSV エディタ ─ [scenario.csv *] ─────────────────────────┐
│ [新規] [開く] [保存] [名前を付けて保存]  文字コード:[utf-8-sig ▾]  [▶ 一括生成…] │
├[ セリフ ][ キャラクター定義 ][ 検証 / 生成ログ ✖0 ⚠2 ]──────────────────────┤
│  #  キャラ  テキスト（原文）  送信テキスト  🔗 出力名   ┃ 絵文字パレット（右枠）│
│  1  あかね  こんにちは。      こんにちは。😊 🔗 ….wav  ┃ [👂 囁き、耳元の音]    │
│  …                                                     ┃ [😮‍💨 吐息、溜息]        │
│                    ← セリフ表（主役・可変）→           ┃ [⏸️ 間、沈黙]          │
│  [＋行追加][複製][↑][↓][削除][原文で上書き][🔊お試し生成]┃ …（縦1列×スクロール）  │
│ ═════ 横スプリッタ（ドラッグで表と絵文字の比率を調整）════ │
└──────────────────────────────────────────────────────────────────────────┘
```

- **セリフタブ**: 左=セリフ表（主役・大きく、横スプリッタで可変）＋行操作。
  右枠=絵文字パレット（**縦1列のグリッド**、各ボタンに `絵文字 + 日本語の意味`、ツールチップに英語）。
- **キャラクター定義タブ**: キャラ表＋追加/削除。
- **検証 / 生成ログタブ**: 検証結果一覧＋進捗バー＋生成ログ（コピー/保存/クリア）。
  隠れていても気づけるよう **タブ見出しにエラー/警告件数バッジ**（例 `✖2 ⚠1`）を表示し、
  **一括生成の開始時はこのタブへ自動で切り替える**。
- タイトルバーに現在のファイル名と未保存マーク `*` を表示。
- セリフ表の `🔗/✎` 列はリンク状態インジケータ（🔗=原文と連動 / ✎=送信テキストを手動編集済み。§5.2.1）。

---

## 5. 機能詳細

### 5.1 キャラクター定義パネル

- 列: `キャラ名` / `参照番号` / `LoRAフォルダ` / `生成ファイルヘッド`。
- **参照番号は自動採番**（追加時に未使用の最小整数を割当）。手動編集も可、重複時は警告。
- `LoRAフォルダ` セルに **[参照…] ボタン**（フォルダ選択ダイアログ）。選択後にパス存在を即検証。
- 行削除時、その参照番号を使うセリフがあれば警告し、削除可否を確認。
- `キャラ名` は表示・選択用（セリフ側ドロップダウンに出す）。

### 5.2 セリフパネル

- 列: `#`（表示専用の通し番号）/ `キャラ`（ドロップダウン）/ `テキスト` / `送信テキスト` / `出力名`。
- **キャラ列はドロップダウン**。表示は `キャラ名`、保存時に `参照番号` へ変換（人は番号を意識しない）。
- **出力名列は読み取り専用**。`生成ファイルヘッド + 連番 + .wav` をライブ計算して表示。
  - 連番は **キャラごと**に、上からの出現順で `1,2,3,…`（ゼロ埋めなし）。
  - 行の並べ替え・キャラ変更に追従して即再計算。
- 行操作: 追加 / 複製 / 上下移動（並び順＝連番順）/ 削除。
- **長文は省略せず全文表示**する（折り返し＋行の高さ自動調整、`…` で切らない）。

#### 5.2.1 原文 → 送信テキストの自動反映（一方向）

- `テキスト`（原文）を入力・編集すると、その内容が `送信テキスト` へ**自動で反映**される。
- `送信テキスト` を編集しても、`テキスト`（原文）へは**反映しない**（逆方向は無効）。
- 反映は **行ごとのリンク状態**で管理する:
  - 初期状態は **リンク（🔗）**。原文の変更が送信テキストへ随時コピーされる。
  - ユーザーが `送信テキスト` を手動編集した時点で、その行は **切り離し（✎）** になり、
    以降は原文を編集しても送信テキストを上書きしない（＝付与した絵文字が消えない）。
  - `✎` の行に **[原文で上書き（再リンク）]** 操作を用意し、明示的に 🔗 へ戻せる。
- 目的: 「原文を打つ → 送信テキストに入る → 絵文字で味付け」という流れを、
  後から原文を直しても絵文字作業を壊さずに回せるようにする。

> 補足: 送信テキストが空のまま保存された行は、CLI 側で原文をフォールバック送信する
> （`SPEC.md` §4.3）。GUI はその旨をツールチップ表示する。

### 5.3 絵文字（顔文字）パレット

- Irodori-TTS は絵文字で感情・話し方・SE 的表現を制御する。パレットの絵文字を
  クリックすると `送信テキスト` へ挿入する（挿入により対象行は ✎ に）。
  挿入位置の確定仕様は §5.3.1。

#### 5.3.1 カーソル位置への挿入（確定仕様）

**目的**: 文の途中（例: 「おは|よう」の `|`）に絵文字を打てること。末尾固定は不可。

**要件（MUST）**

| # | 状態 | ボタン押下時の挙動 |
|---|---|---|
| R1 | 送信テキストのセルを**編集中**（セルエディタ=QLineEdit が開いている） | エディタ内の**現在のカーソル位置**に絵文字を挿入する。**エディタは閉じない**。 |
| R2 | R1 で**テキストが選択状態**（編集開始直後の全選択を含む） | 選択を**置換しない**。選択を解除し、カーソル位置に**挿入のみ**行う（既存文字を1文字も消さない）。 |
| R3 | R1 の後、**続けて**別の絵文字を押下 | 直前の挿入の**直後の位置**に挿入される（カーソルが追従している）。 |
| R4 | セルを**編集していない**（行を選択しているだけ） | その行の送信テキストの**末尾**に追加する。 |
| R5 | 行を選択していない | 挿入せず、案内メッセージを表示する（「セルをダブルクリックして編集し、入れたい位置にカーソルを置いてから押してください。」）。 |
| R6 | いずれの挿入でも | 対象行のリンク状態を **✎（切り離し）** にする（§5.2.1。以後、原文編集で上書きされない）。 |
| R7 | **原文(テキスト)セルを編集中**（未確定）にボタン押下 | まず原文を**確定**し（＝送信テキストへ反映、消失させない）、その反映後の送信テキスト末尾へ絵文字を追記する。例: 原文に「こんにちは」入力中 → 😊 → 送信テキスト「こんにちは😊」、原文は消えない。 |

**実装ノート（二重の堅牢化で確実に成立させる）**

過去、絵文字クリックでセルエディタがフォーカスを失って閉じ、末尾追加へ退行した
（コミット `03300a6`→`b5d6833`→`6422e37` 参照）。次の 2 系統を**両方**用意して確実にする。

1. **パレットをフォーカス透過にして、編集中エディタを閉じさせない（ライブ挿入経路）**
   - 絵文字ボタンだけでなく、パレットの **`QScrollArea`・その `viewport()`・ラッパー `QWidget`**
     も含めて **`Qt.NoFocus`** にする（ボタン単体の NoFocus だけでは、親のスクロール
     エリア等がクリックでフォーカスを奪い、エディタが閉じてしまう）。
   - 送信テキスト列のデリゲートは、編集中の QLineEdit 参照をウィンドウへ通知する
     （`createEditor` で通知、`destroyEditor` で None）。
   - R1/R2 は `editor.deselect()` → `editor.insert(emoji)` の順（`insert()` は選択範囲を
     置換するため、必ず先に `deselect()`）。
2. **カーソル位置を追跡し、万一エディタが閉じても正しい位置へ挿入（保険経路）**
   - デリゲートは `cursorPositionChanged` と `setModelData` で **(row, pos)** を通知し、
     ウィンドウが最後の位置を保持する。
   - エディタ参照が None（閉じている）のときは、記録した (row, pos) を使い
     `LineTableModel.insert_emoji(row, emoji, pos)` でモデル層へ挿入する。
   - `insert_emoji(row, emoji, pos=None)` は `pos=None`=末尾、範囲外はクランプ（単体テスト対象）。

- どちらの経路でも、絵文字は複数コードポイント（結合絵文字 `😮‍💨` 等）があるため、位置計算は
  **str 連結／QLineEdit の挿入 API に任せ**、自前でコードポイントを数えない。

**R7（原文編集中の消失防止）の実装ノート**

- 原文(テキスト)列にも編集中エディタを追跡するデリゲート（`EditorTrackingDelegate`）を付け、
  その QLineEdit 参照を保持する。
- 絵文字クリック時、送信テキストを編集中でなければ、保持している原文エディタを
  `view.commitData()` → `closeEditor()` で**確定**してから追記する（`focusWidget()` に依存しない）。
- なぜ必要か: モデルの `insert_emoji` が `dataChanged` を出すと、編集中セルのエディタが
  モデルの古い値でリセットされ、未確定入力が消える。確定を先に行うことでこれを防ぐ。
  併せて `insert_emoji` の `dataChanged` は**変更列（送信/リンク）だけ**に限定する（保険）。

**受け入れ基準（実装後に必ず確認）**

1. `おはよう` を編集しカーソルを「おは|よう」に置いて 😊 → `おは😊よう`（R1）
2. 編集開始直後（全選択のまま）に 😊 → `おはよう😊`（消えない・R2）
3. 続けて 😲 → `おは😊😲よう`（R3、1. の続きなら）
4. エディタが開いたままである（R1）
5. 未編集で行選択のみ → 末尾に追加（R4）
6. 行未選択 → ダイアログ表示、テキスト不変（R5）
7. 挿入した行の 🔗 が ✎ になる（R6）

**自動テスト**: R4/クランプは `tests/test_gui_models.py`（Qt 不要）。
R1〜R3 はヘッドレス（`QT_QPA_PLATFORM=offscreen`）で `line_view.edit()` →
エディタに `setCursorPosition()` → `_insert_emoji()` を呼んで検証する。
- 収録は **付録 B の 39 種**（`emoji_palette.tsv` を読み込む）。各ボタンは絵文字を表示し、
  **ツールチップに意味（日本語）** を出す（例 🥺 →「声を震わせながら、自信のなさげに」）。
- パレットは `emoji_palette.tsv`（`emoji / meaning_ja / meaning_en` の TSV）を編集して増減できる。

### 5.4 ツールバー / ファイル操作

- `新規`（Ctrl+N）/ `開く`（Ctrl+O）/ `保存`（**Ctrl+S**）/ `名前を付けて保存`（Ctrl+Shift+S）。
  ショートカットは `Qt.ApplicationShortcut` で、ツールバー非表示時も効く。
- `文字コード` セレクタ: `utf-8-sig`（既定, Excel 互換）/ `utf-8` / `cp932`。保存時に反映。
- **未保存のまま 終了 / 新規 / 開く をしようとしたら、`保存 / 破棄 / キャンセル` を尋ねる**
  （`_maybe_save`）。
  - 保存 → 実際に保存できたら続行（新規ファイルは名前付け保存。中止したら中断）。
  - 破棄 → 保存せず続行。
  - キャンセル → 操作を中断（終了しない）。
  - 変更が無ければ尋ねずに続行。

### 5.5 一括生成（フォルダ出力）と進捗表示

GUI から CLI の `build` を起動し、**選んだフォルダに全音声と CSV をまとめて出力**する
（生成本体は CLI が担当）。出力フォルダは音声データと元 CSV を含む**再現可能なバンドル**になる。

- **[▶ 一括生成…] ボタン**押下でフロー:
  1. 未保存があれば保存を促す（生成は保存済み CSV を対象にするため）。
  2. 検証にエラーがあれば警告し、続行可否を確認。続行する場合でも、CLI 側で未捕捉例外にせず、
     生成ログに読めるエラーとして出す。
  3. **出力先フォルダを選択**するダイアログを表示。
  4. CLI を **サブプロセス起動**する。例:
     `{python} -m irodori_cli build --config {config} --csv {保存先csv}
      --out-dir {選択フォルダ} --copy-csv --group-by-char --progress`。
  5. 標準出力/標準エラーを逐次読み取り、**生成ログパネル**に流す。
- **出力フォルダの内容**（`SPEC.md` build --out-dir / --copy-csv / --group-by-char）
  ＝ 音声は **キャラごとのサブフォルダ** に分かれる。サブフォルダが異なれば同じファイル名を許可する:

  ```
  <選択フォルダ>/
    scenario.csv        # 生成に使った CSV のコピー
    あかね/             # キャラ名のサブフォルダ
      akane_1.wav
      akane_2.wav
    ゆい/
      yui_1.wav
      yui_2.wav
  ```

- **進捗表示**: 進捗バー（`生成 N / 全 M`）＋現在の出力ファイル名。
  - CLI は `--progress` で 1 行 1 イベント（例 `PROGRESS 12/18 akane_12.wav GENERATED`）を出力し、
    GUI がパースしてバー更新する（`SPEC.md` §8 の進捗プロトコル）。
- **[中止] ボタン**でサブプロセスを終了。生成済み分は保持される。
- 完了時に「生成 M 件 / スキップ K 件 / 失敗 F 件」＋出力フォルダのパスをサマリ表示
  （[フォルダを開く] ボタン付き）。失敗一覧はログに残す。空テキスト等の入力起因エラーも、
  Python のスタックトレースではなくユーザー向けメッセージとして残す。
- 生成中は編集操作を抑止（多重起動防止）。
- 生成ログには **[ログをコピー]**（クリップボード）/ **[ログを保存…]**（テキストファイル）/ **[クリア]**
  を備え、エラー内容を容易に共有できるようにする（子プロセスの出力はマージして取り込む）。

> `config.yaml`（`irodori.repo_dir` など実行環境）と Python 実行コマンドは設定画面 or
> config から取得する。GUI は infer.py の引数を直接は組み立てない（CLI に委譲）。

### 5.6 選択行のお試し生成（試聴）

本番フォルダを汚さずに、**選択中の 1 行**の音声を即生成して声を確認する。

- **[🔊 お試し生成] ボタン**（セリフパネル）押下でフロー:
  1. 選択行の `送信テキスト`（空なら原文）と、参照番号から解決した **LoRA フォルダ**を取得。
  2. CLI を起動: `{python} -m irodori_cli preview --text {送信テキスト}
     --lora-dir {LoRAフォルダ} --out {プロジェクト}/preview/rowN.wav`（`SPEC.md` preview）。
  3. 返ってきた wav を **QtMultimedia で自動再生**（[再生]/[停止] を提供）。
- 出力は **プロジェクト内の見える `preview/` フォルダ**（隠し temp は使わない。存在を忘れないため）。
  **本番の出力フォルダ・state は変更しない**。
- キャッシュは共有するため、本番生成時に同一テキストなら再生成されず速い。
- 生成中はボタンをスピナー表示にし、失敗時はエラーをダイアログ表示。
- 複数行を選択している場合は先頭行のみ（または順次生成して並べて再生）。既定は先頭行。

---

## 6. ファイル入出力（ラウンドトリップ）

### 6.1 保存

`SPEC.md` §4 の形式で出力する。

- 定義セクション → 空行 → セリフセクションの順。各セクションにヘッダ行。
- ヘッダ名は `SPEC.md` §4.2 / §4.3 の列名に一致させる
  （定義: `キャラ名,参照番号,LoRAフォルダ,生成ファイルヘッド` / セリフ: `参照番号,テキスト,送信テキスト`）。
- キャラのドロップダウン選択は `参照番号` に変換して書き出す。
- カンマ・改行・`"` を含む値は標準 CSV クォート。
- 選択した文字コードでエンコード（既定 `utf-8-sig`）。

### 6.2 開く

- 文字コードを自動判定（BOM 優先、失敗時は設定エンコードで再試行）。
- 2 セクションを **ヘッダ列名で識別**して読み分ける（順序不問、空行区切り）。
- 参照番号 → キャラ名を解決してドロップダウン表示に反映。
- 準拠しない CSV はエラー箇所を提示して読み込み中断（部分読込はしない）。

---

## 7. データモデル（内部）

```jsonc
{
  "characters": [
    { "name": "あかね", "ref": 1, "lora_dir": "C:\\lora\\akane", "head": "akane_" }
  ],
  "lines": [
    { "ref": 1, "text": "こんにちは。", "send_text": "こんにちは。😊", "linked": false }
  ]
}
```

- 出力名はモデルに保持せず、`characters[ref].head + 連番 + ".wav"` で都度算出（唯一の真実源＝並び順）。
- `linked`（§5.2.1）: `true`=原文と連動（🔗）/ `false`=送信テキスト手動編集済み（✎）。
  CSV には保存されない **GUI 内部状態**（開いた直後は、原文と送信テキストが一致すれば `true`、
  異なれば `false` と推定する）。

---

## 8. 検証（リアルタイム＋保存前）

`SPEC.md` §4.6 と同一ルールを画面上で先取りチェックする。

| 種別 | 条件 | 表示 |
|---|---|---|
| エラー | 参照番号の重複 | 定義行を赤表示 |
| エラー | セリフの参照番号が未定義 | 該当行を赤表示 |
| エラー | 出力名の重複 | 両行を赤表示 |
| エラー | `送信テキスト`・原文ともに空 | 該当行を赤表示 |
| 警告 | `LoRAフォルダ` が実在しない | 定義セルを黄表示 |
| 警告 | `生成ファイルヘッド` が空 | 定義セルを黄表示 |
| 警告 | `キャラ名` の重複（一括生成のサブフォルダが衝突） | 定義セルを黄表示（§5.5） |

- 検証パネルに件数と一覧を表示。項目クリックで該当セルへジャンプ。
- **エラーが残る状態でも保存は可能**（作業途中の保存を許す）が、保存時に警告を出す。
  CLI 側 `validate` が最終ゲートとなる。

---

## 9. CLI との共有

- CSV の**読み書き・検証・出力名算出**は共有モジュール（例 `irodori_csv/`）に実装し、
  CLI（`build`/`validate`）と GUI の両方が参照する。
- これにより形式・検証ルールの二重管理とズレを防ぐ。GUI は薄い表示層に徹する。

想定パッケージ構成:

```
irodori_csv/        # 共有: モデル / パーサ / 直列化 / 検証 / 出力名算出
irodori_cli/        # CLI: validate / build / clean（infer.py 実行）
irodori_gui/        # GUI: PySide6 アプリ
```

---

## 10. 配布

- PyInstaller で Windows 単一 `.exe` を生成（`irodori-csv-editor.exe`）。
- Python 未インストール環境でもダブルクリック起動できることを目標とする。

---

## 11. 未確定事項・将来拡張

- ~~A. GUI フレームワーク~~ → **PySide6 で確定**。
- ~~B. 「生成を実行」ボタン~~ → **スコープに追加済み（§5.5）**。
- **C. 行のドラッグ＆ドロップ並べ替え / Undo・Redo**。
- **D. 顔文字パレットのカスタマイズ UI**（現状は `emoji_palette.tsv` の手編集で対応）。
- **E. 参照音声（`--ref-wav`）や caption 列**を扱う拡張（`SPEC.md` の将来拡張に追従）。
- **F. Mac/Linux 配布**。

---

## 付録 A: 参照

- CSV 本体仕様: [`SPEC.md`](./SPEC.md) §4
- Irodori-TTS（絵文字スタイル制御）: https://github.com/Aratako/Irodori-TTS

## 付録 B: 絵文字パレット（`emoji_palette.tsv`）

送信テキストに挿入して感情・話し方・SE 的表現を制御する。GUI はこの TSV を読み込み、
各ボタンのツールチップに意味（日本語）を表示する。列は `emoji / meaning_ja / meaning_en`。

| 絵文字 | 意味（日本語） | Meaning (EN) |
|---|---|---|
| 👂 | 囁き、耳元の音 | Whisper, sounds close to the ear |
| 😮‍💨 | 吐息、溜息、寝息 | Breath, sigh, sleeping breath |
| ⏸️ | 間、沈黙 | Pause, silence |
| 🤭 | 笑い（くすくす、含み笑いなど） | Chuckle, giggle, suppressed laugh |
| 🥵 | 喘ぎ、うめき声、唸り声 | Panting, moan, groan |
| 📢 | エコー、リバーブ | Echo, reverb |
| 😏 | からかうように、甘えるように | Teasing, playfully sweet / coaxing |
| 🥺 | 声を震わせながら、自信のなさげに | Trembling voice, timidly / uncertainly |
| 🌬️ | 息切れ、荒い息遣い、呼吸音 | Shortness of breath, heavy breathing |
| 😮 | 息をのむ | Gasp |
| 👅 | 舐める音、咀嚼音、水音 | Licking sound, chewing sound, wet sound |
| 💋 | リップノイズ | Lip smack / lip noise |
| 🫶 | 優しく | Gently, tenderly |
| 😭 | 嗚咽、泣き声、悲しみ | Sobbing, crying, sorrowfully / sadly |
| 😱 | 悲鳴、叫び、絶叫 | Scream, shout, shriek |
| 😪 | 眠そうに、気だるげに | Sleepily, sluggishly / languidly |
| ⏩ | 早口、一気にまくしたてる、急いで | Fast-speaking, rapid-fire, hurriedly |
| 📞 | 電話越し、スピーカー越しのような音 | Over the phone, through a speaker |
| 🐢 | ゆっくりと | Slowly |
| 🥤 | 唾を飲み込む音 | Gulp, swallowing sound |
| 🤧 | 咳き込み、鼻をすする、くしゃみ、咳払い | Coughing, sniffling, sneeze, clearing throat |
| 😒 | 舌打ち | Tutting, clicking tongue |
| 😰 | 慌てて、動揺、緊張、どもり | Panicked, agitated, nervous, stuttering |
| 😆 | 喜びながら | Joyfully, happily |
| 😠 | 怒り、不満げに、拗ねながら | Angry, displeased, sulking |
| 😲 | 驚き、感嘆 | Surprise, awe / exclamation |
| 🥱 | あくび | Yawn |
| 😖 | 苦しげに | Painfully, agonizingly |
| 😟 | 心配そうに | Anxiously, worriedly |
| 🫣 | 恥ずかしそうに、照れながら | Shyly, bashfully |
| 🙄 | 呆れたように | Exasperatedly, rolling eyes |
| 😊 | 楽しげに、嬉しそうに | Cheerfully, gladly |
| 👌 | 相槌、頷く音 | Backchanneling, sound of agreement |
| 🙏 | 懇願するように | Pleadingly, begging |
| 🥴 | 酔っ払って | Drunkenly |
| 🎵 | 鼻歌 | Humming |
| 🤐 | 口を塞がれて | Muffled (mouth covered) |
| 😌 | 安堵、満足げに | Relieved, contentedly |
| 🤔 | 疑問の声 | Questioning voice, wondering |
